
Update github settings for ci-testing so that when a branch's workflow completes
it is automatically merged to main.

In the target repos Settings
Scroll down to Pull-Requests
Allow auto-merge. Tick it
Automatically delete head branches. Tick it.

In the target repos Settings
On lhs, Actions, General
Scroll down
"Allow GitHub Actions to create and approve pull requests" Tick it
Save


